# 设计说明

## 范围：只管画面

```
参考视频
   ↓  自动切分镜 + 细切 + 手动补刀          ← 本地 ffmpeg，零费用
分镜时间线
   ↓  人工标注 + AI 初判（景别/运镜/构图/光线）
结构化拉片数据
   ├─→ Markdown 拉片笔记           给人看
   └─→ 复刻包 → Remotion 代码模板   给 AI 复刻用
                    ↓
              用模板做视频（中文表单 + 实时预览 + 一键导出）
```

工具只处理画面：镜头边界、构图、景别、运镜、元素位置、动效。
声音（背景音乐、人声）和口播字幕在后期里加，不归这个工具管。

早期版本做过逐词转写、BGM/BPM 识别和 hypit SVML 导出，已全部移除：
转写和音频不属于画面；SVML 的核心是「把口播按词锚定」，口播不要了它就只剩空壳，复刻改走 Remotion。
老项目文件里残留的 `words` / `audio` 字段在读取时剥掉，镜头和标注不受影响。

## 贯穿全局的设计约束

### 1. 标注来源必须字段级可追溯

`Shot.annotationSource` 记录每个字段是 `manual` / `ai` / `ai-edited`。

没有它，AI 批量标注跑第二轮就会把人工修正冲掉，而拉片的人工投入是以小时计的。
粒度必须到字段，不能到镜头：同一个镜头的景别可能是 AI 判的，运镜是人改的。

### 2. 切分可以反复改，标注不能丢

自动检测不可能完全准：同一条片子里，真人画面和录屏演示的场景分数差一个数量级，
一个全局阈值永远调不对。所以切分是三层兜底：全局自动 → 单镜头细切 → `S` 手动补刀 / `M` 合并。

任何一层改了边界，已有标注都要跟着走：
- 重新切分：`carryOverAnnotations` 按时间重叠迁移，重叠不足新镜头一半就不继承，宁可空着也不张冠李戴
- 补刀：前半段保留全部标注，后半段只继承 A/B-roll 归类
- 合并：以前一个镜头的判断为准，只累加元素、特效、备注，并撤销「已审」

### 3. 不确定就不编

- AI 返回的枚举值一律过白名单校验，编造的值直接丢弃
- 比对工具在总分达标但局部漂移时单独报告（退出码 3），不让总分掩盖问题

### 4. 复刻：第一指标是像，而且要能被测量

没有可测量的目标，AI 改完只能自己说「像了」。所以复刻包里一定带原片片段和逐帧序列，
比对工具逐帧打分、分格检测漂移、生成差异视频。原片里不属于复刻范围的东西（口播字幕）用 `--mask` 遮掉。

### 5. 模板：用户只碰简单参数

模板组件的参数是给代码用的（贝塞尔曲线、帧号、像素），用户看不懂也不该碰。
每个模板另写一份 `params.ts`：简单参数（几秒出现、从哪进来、什么颜色）、中文表单描述、换算函数。
界面只编辑简单参数，作品里也只存简单参数——以后换算改进了（比如曲线测得更准），老作品重新导出就自动用上。
换算的默认值必须复现原片实测（有测试守着）。

### 6. Mac 软件：程序只读，数据在「文稿」

软件里自带 ffmpeg、yt-dlp、渲染用的浏览器和打包好的模板，全部通过环境变量交给工作台服务，
服务代码本身不区分「开发」还是「软件」。用户数据统一走 `core/paths.ts`，
软件里是「文稿/ShotPilot」下的中文文件夹，更新软件（整个替换 .app）不会碰到用户的作品。

## 模块划分

| 模块 | 职责 |
| --- | --- |
| `core/vocabulary.ts` | 标注词汇表。快捷键唯一性、保留键不被占用由测试保证 |
| `core/types.ts` | 数据模型 |
| `core/project.ts` | 项目读写。**原子写盘**（临时文件 + rename），防止写到一半损坏 |
| `analyze/ffmpeg.ts` | 二进制解析。会校验文件真实存在，因为 ffmpeg-static 的 postinstall 在代理环境常静默失败 |
| `analyze/shots.ts` | 场景检测、镜头构建、补刀、合并、细切、标注迁移 |
| `analyze/thumbnails.ts` | 缩略图。按起始时间命名，镜头重新编号时不用改文件 |
| `analyze/vision.ts` | AI 初判。多帧送模型、输出过白名单校验 |
| `export/notes.ts` | Markdown 拉片笔记 |
| `export/handoff.ts` | 给 AI 的结构化拉片数据 |
| `export/replica.ts` | 复刻包：原片片段、逐帧序列、画面标注、给 agent 的指令 |
| `export/compare.ts` | 复刻比对：逐帧 SSIM、分格漂移检测、遮罩、差异视频 |
| `export/render.ts` | 用模板导出视频：加载打包好的模板、补全图片地址、调 Remotion 渲染器 |
| `core/paths.ts` | 用户数据放在哪（开发时在项目目录，软件里在「文稿/ShotPilot」） |
| `core/works.ts` | 我的作品（原子写盘）、上传的图片（按内容取名去重） |
| `templates/src/` | 模板组件 + 简单参数/中文表单/换算，`registry.ts` 统一登记 |
| `web/src/` | 整个界面（React + Remotion 播放器），打包成 `web/js/app.js`。照 macOS 的样子：左边工具栏、中间预览、右边参数、底下时间轴。`ui/` 是按钮、菜单、弹出面板这些基础控件；`clip/` 拉片；`templates/` 选模板；`works/` 作品和模板编辑 |
| `templates/src/*/params.ts` 的 `timeline` | 模板编辑器底部时间轴：怎么从参数画出色块、拖完怎么换算回参数（纯函数，有测试） |
| `core/library.ts` | B-roll 素材标签：用户的打标模板、文件名公式、Eagle 标签和注释（纯函数，界面预览和放进 Eagle 用同一套） |
| `analyze/library-tagger.ts` | AI 看画面打素材标签。关系、身份、具体事件不问 AI，永远等人确认 |
| `export/eagle.ts` | 放进 Eagle（本机接口 localhost:41595）：建文件夹、放片段、再放一次是更新，只换自己打的标签 |
| `core/settings.ts` | 设置（看图 AI 的 Key、Eagle 令牌），存在「ShotPilot」文件夹里 |
| `desktop/` | Mac 软件：主程序、打包脚本、冒烟测试 |
| `server/` | 本地 HTTP。**必须支持 Range**，否则时间线拖拽不可用 |
